{
  "name": "nodeless",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "predeploy": "cd node_modules && rd /s /q sharp && npm install --platform=linux --arch=x64 --target=8.10.0 sharp",
    "deploy": "serverless deploy -v",
    "postdeploy": "cd node_modules && rd /s /q sharp && npm install sharp"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sharp": "^0.28.1"
  }
}
